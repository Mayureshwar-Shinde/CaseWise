.text-danger
  - if resource.errors.any?
    %h4 #{resource.errors.count} errors prohibited this #{resource.class} from being saved:
    %ul
      - resource.errors.full_messages.each do |message|
        %li= message